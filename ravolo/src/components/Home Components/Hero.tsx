import HeroMessage from "./HeroMessage"
import HomeNav from "./HomeNav"

const Hero = () => {
  return (
    <section>
        <HomeNav/>
        <HeroMessage/>
    </section>
  )
}

export default Hero
